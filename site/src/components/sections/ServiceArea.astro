---
import MapPin from "@tabler/icons/outline/map-pin.svg";

import SectionHeader from "../common/SectionHeader.astro";
import Roofline from "../common/Roofline.astro";
import { business, serviceAreaCities, serviceAreaCounties } from "../../data/business";

const countiesSentence = serviceAreaCounties.join(", ").replace(/, ([^,]*)$/, ", and $1");
---

<section id="service-area" class="section-pad scroll-mt-16">
  <div class="mx-auto max-w-6xl px-4 sm:px-6">
    <SectionHeader
      title="Our Service Area"
      lead={`Based in Metro Detroit and working across it — homes, rentals, and portfolios throughout ${countiesSentence}.`}
    />

    <div class="mt-12 grid items-stretch gap-6 lg:grid-cols-2">
      <div class="reveal border-border overflow-hidden rounded-xl border bg-white shadow-card">
        <iframe
          src="https://www.openstreetmap.org/export/embed.html?bbox=-83.85%2C42.00%2C-82.60%2C42.90&layer=mapnik&marker=42.383%2C-83.102"
          title="Map of the KOM USA service area across Metro Detroit"
          class="h-full min-h-80 w-full border-0"
          loading="lazy"
          referrerpolicy="no-referrer-when-downgrade"></iframe>
      </div>

      <div class="reveal flex flex-col justify-center" style="--rd: 100ms">
        <ul class="flex flex-wrap gap-2.5">
          {
            serviceAreaCities.map((city) => (
              <li class="border-border text-kom-ink flex items-center gap-1.5 rounded-full border bg-white px-3.5 py-1.5 text-sm font-medium">
                <MapPin class="text-kom-field size-3.5" aria-hidden="true" />
                {city}
              </li>
            ))
          }
          <li class="text-muted-foreground flex items-center px-2 py-1.5 text-sm font-medium">
            …and surrounding Metro Detroit communities
          </li>
        </ul>

        <Roofline class="text-kom-tan mt-8" />
        <p class="text-muted-foreground mt-4 max-w-xl text-sm leading-relaxed">
          Not sure if we serve your city? <a href={business.phoneHref} class="text-kom-field font-bold hover:underline">Call</a> or <a href={business.smsHref} class="text-kom-field font-bold hover:underline">text us</a> at {business.phone} and we'll tell you right away. Locksmith jobs over 25 miles from our location add a $50 distance fee.
        </p>
      </div>
    </div>
  </div>
</section>
