/**
 * Definition of the offer form value sent with lead submissions.
 * business.ts imports this for offer.formValue — this file is the source of truth
 * so client-side bundles (e.g. CouponOffer) don't need to pull in all of business.ts.
 */
export const offerFormValue = "$10 first-service offer claimed";
