---
import { featuredServiceAreas } from "../../data/service-areas";

export function getStaticPaths() {
  return featuredServiceAreas.map((area) => ({ params: { slug: area.slug } }));
}

return Astro.redirect(`/service-areas/${Astro.params.slug}/`, 301);
---
